import { UnSelectedStateDirective } from './un-selected-state.directive';

describe('UnSelectedStateDirective', () => {
  it('should create an instance', () => {
    const directive = new UnSelectedStateDirective();
    expect(directive).toBeTruthy();
  });
});
