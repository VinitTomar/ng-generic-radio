import { SelectedStateDirective } from './selected-state.directive';

describe('SelectedStateDirective', () => {
  it('should create an instance', () => {
    // const directive = new SelectedStateDirective();
    // expect(directive).toBeTruthy();
  });
});
